// Shared so the server (which persists the AI's leaderboard row and overrides the AI
// seat's stored name) and the client (which displays it) agree on one exact string.
export const AI_PLAYER_NAME: string = 'AI Dices BOT';
