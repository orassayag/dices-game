node_modules/
dist/
.env
.env.local
.env.*.local
coverage/
*.log
.DS_Store
Thumbs.db

# Workflow-owned working drafts — never commit (docs/plans/ holds the finalized output)
docs/pre-plans/

# Claude Code local/machine-specific state — never commit
.claude/settings.local.json
.claude/status/

# Secrets & credentials — never commit (workflow-wide hygiene)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.keychain
*.keychain-db
*.dump
*.sql.gz
